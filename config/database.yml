# SQLite. Versions 3.8.0 and up are supported.
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
#
default: &default
  adapter: postgresql
  schema_search_path: "public,shared_extensions"
  encoding: utf8
  host:     <%= ENV.fetch('DATABASE_HOST', 'localhost') %>
  user:     <%= ENV.fetch('DATABASE_USER', 'postgres') %>
  password: <%= ENV.fetch('DATABASE_PASSWORD', 'postgres') %>
  sslmode:  <%= ENV.fetch('DATABASE_SSL_MODE', nil) %>
  # For details on connection pooling, see rails configuration guide
  # http://guides.rubyonrails.org/configuring.html#database-pooling
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  primary:
    schema_search_path: "public,shared_extensions"
    host:     <%= ENV.fetch('DATABASE_HOST', 'localhost') %>
    user:     <%= ENV.fetch('DATABASE_USER', 'postgres') %>
    password: <%= ENV.fetch('DATABASE_PASSWORD', 'postgres') %>
    sslmode:  <%= ENV.fetch('DATABASE_SSL_MODE', nil) %>
    adapter: postgresql
    database: multiCompaniesDb
  firsttenant:
    adapter: postgresql
    schema_search_path: "public,shared_extensions"
    user:     <%= ENV.fetch('DATABASE_USER', 'first_tenant') %>
    password: <%= ENV.fetch('DATABASE_PASSWORD', 'postgres') %>
    database: firsttenant
    migrations_paths: db/first_tenant_migrations
  secondtenant:
    adapter: postgresql
    schema_search_path: "public,shared_extensions"
    user:     <%= ENV.fetch('DATABASE_USER', 'second_tenant') %>
    password: <%= ENV.fetch('DATABASE_PASSWORD', 'postgres') %>
    database: secondtenant
    migrations_paths: db/second_tenant_migrations
  # amine:
  #   adapter: postgresql
  #   schema_search_path: "public,shared_extensions"
  #   user:     <%= ENV.fetch('DATABASE_USER', 'postgres') %>
  #   password: <%= ENV.fetch('DATABASE_PASSWORD', 'postgres') %>
  #   database: firsttenant
  #   migrations_paths: db/Bassem/migrations
  # third_DB:
  # adapter: postgresql
  # schema_search_path: "public,shared_extensions"
  # user:     <%= ENV.fetch('DATABASE_USER', 'postgres') %>
  # password: <%= ENV.fetch('DATABASE_PASSWORD', 'postgres') %>
  # database: second_tenant_dev
  # migrations_paths: db/third_db_migrations

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
# test:
#   <<: *default
#   database: multiCompaniesdDb

production:
  <<: *default
  database: multiCompaniesdDb
